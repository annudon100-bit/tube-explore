services:
  tube-explore:
    build: .
    ports:
      - "8000:8000"
    environment:
      CONFIG_DIRECTORY: /app/config
      DOWNLOAD_DIRECTORY: /app/downloads
    volumes:
      - ./downloads:/app/downloads
      - ./config:/app/config

volumes:
  downloads:
  config:
